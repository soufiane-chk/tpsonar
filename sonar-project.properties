# Mandatory
sonar.projectKey=tpsonar

# Project metadata
sonar.projectName=tpsonar
sonar.projectVersion=1.0

# Path settings
sonar.sources=src                 # Adjust if source files are located elsewhere
sonar.tests=tests                  # Optional: for test files
sonar.java.binaries=target/classes # Adjust based on where compiled files are

# Authentication (Replace with your actual token)
sonar.host.url=http://localhost:9000
sonar.login=sqa_0c544930724a7b73974241d559e8c341bb390934 # Replace with the actual token

# Optional: Language specifics (adjust if your project isn't Java)
sonar.language=java
